# Platform / Super-Admin Audit — 2026-06-11

Method: 5 agents read all 16 platform pages + 9 gym-detail tabs + layout/routes in full (~470KB), verified every column/RPC/RLS policy against migrations, traced notification producers, checked i18n in both locales, and audited the math on every analytics formula. file:line refs relative to `gym-app/`. VERIFIED = root cause confirmed in code/migrations.

Status: `[ ]` open · `[x]` fixed · `[-]` won't fix
Note: maintenance mode (Operations card + MaintenanceGate + 0539) was rebuilt the same day — excluded from findings.

---

## ✅ RESOLUTION — 2026-06-11 fix wave (same day)

**ALL P0 (3), P1 (7), P2 (15) + the P3 batch fixed** by 1 lead + 6 parallel agents. Highlights: platform_create_gym RPC + invite management + set-owner UI (onboarding works end-to-end); content CRUD rebuilt on the REAL schemas in both GymDetail and PlatformSettings; the 7 kill switches are now REAL (get_platform_flags RPC + usePlatformFlags hook gating Referrals/Classes/Social/Messages/QR-display/Challenges/Nutrition member-side, fail-open); churn numbers deduped to latest-per-member-7d everywhere (0540) + avg_churn_velocity powers a real Rising Churn card; FeatureAdoption on a SECURITY DEFINER RPC (0541); GymDiagnostic right-censored + local-date parsing + full ES + print button; platform_snapshots weekly cron feeds a REAL health trend, honest income chart, and WoW deltas; deletion executor + 90d error retention + import batch rollback (0545); cross-gym password reset + email lookup + real invite-code tooling + audit target-gym attribution (0543); cards notifications audience fix + crash alerts now PUSH + /admin deep links work via auto view-switch (0544); error resolve workflow + honest failure states platform-wide; CSV exports on the three analytics surfaces.

**PENDING USER ACTIONS**
1. Apply migrations **0540, 0541, 0542, 0543, 0544, 0545, 0547** (0546 intentionally unused) — plus the earlier **0528–0539** if not yet applied.
2. Redeploy the 10 edge functions from the ai_rate_limits fix (still pending).
3. Xcode Run (build green, cap copy done).

**DEFERRED (explicit, small list)**
Impersonation/login-as (security design needed); revenue ledger beyond snapshots; per-gym scope selectors on GymHealth/FeatureAdoption; GymOps/GymImport full i18n (deliberate English operator surfaces).

---

## P0 — BROKEN CORE FLOWS (the founder's own workflows)

- [x] **P0-1 Gym onboarding is broken end-to-end.** (a) GymCreateModal's owner invite is silently never created — `gym_invites` INSERT policy requires `gym_id = current_gym_id()` (0022:36), which is the founder's own gym, never the new one; `{ error }` discarded → "Gym created successfully" toast anyway (GymCreateModal.jsx:96–105). (b) `platform_create_gym` RPC exists in NO migration (only 0363's exempt list) and the fallback trigger regexes `message` for codes PostgREST puts in `error.code` — if the live DB lacks a hand-made RPC, Create Gym surfaces a raw error and the fallback never runs (GymCreateModal.jsx:63). (c) The import "Download codes sheet" button is dead — selects `profiles.phone`; real column is `phone_number` (GymImport.jsx:181,191,206; 0466 fixed this same mistake inside the RPC but not here); silent return, no CSV. (d) No platform UI can create/copy/revoke invites at all, and `gyms.owner_user_id` is written by no code path (Owner column permanently "—"). Net: no working path to hand a new gym its owner login or its member codes.
- [x] **P0-2 Content CRUD was written against an imagined schema — every Add fails, every failure reports success.** Same defect in TWO places:
  - GymDetail content tab (GymDetail.jsx:384–390): challenges insert omits NOT NULL `created_by` + offers enum value `'pr'` that doesn't exist (real: pr_count) + allows null start/end (NOT NULL); programs write `difficulty_level` — column exists in zero migrations → insert AND edit always fail; achievements write `type`/`requirement_value` — neither column exists (real: name/description/icon/category/criteria). All handlers ignore { error } and close the modal.
  - PlatformSettings (PlatformSettings.jsx:1574–1586, 1631–1643): Add Achievement same imagined columns + missing NOT NULL icon/category; Add Program Template also misses NOT NULL days_per_week AND has no super_admin RLS write policy on program_templates at all (only own-gym admin, 0002:232) — insert + delete both silently no-op.
- [x] **P0-3 The entire platform config surface is decorative.** All 14 feature flags (7 Operations kill switches + 7 PlatformSettings flags, plus 2 seeded orphans), 3 email toggles, and the gym-defaults form write `platform_config` keys that NOTHING in src/ or any edge function reads (repo-wide grep; only reader is MaintenanceGate's RPC). The "kill switches" come with a scary "affects all gyms" confirm — flipping one in a real incident does nothing. Also: platform_config RLS is super_admin-only SELECT, so member-side gates couldn't even read these flags as written. Operations vs Settings also use DIFFERENT keys for the same concepts (referrals↔referralSystem, social↔socialFeed, classes↔classBooking).

## P1 — WRONG DATA ON DECISION PAGES

- [x] **P1-1 Every churn number at platform level aggregates the full HISTORY of churn_risk_scores** — no computed_at filter, no latest-per-member dedup in `platform_gym_stats` / `platform_churn_signals` (0437/0438) or `platform_gym_attention` (0435). A member at-risk 30 days counts 30×; inflation grows daily (append-per-day table, no pruning since the one-time 0510 purge). Affects: Total At-Risk, churn % (can exceed 100%), Struggling Gyms flags, Avg Churn charts, GymHealth's churn component (15% of every health score), Attention's churn problem rows. The admin tier does it correctly (loadScores.js: 7d window + latest per member) → platform never matches gym dashboards.
- [x] **P1-2 FeatureAdoption is mostly RLS-dead.** 8 of 12 feature reads (gym_classes, win_back_attempts, conversations, referral_codes, referral_milestones, nps_responses, announcements, member_segments) + admin_presence have NO cross-gym super_admin SELECT arm → silently empty → heatmap says "Never used" for features gyms use daily; the whole Admin Engagement section ≈ founder's own gym only. GymHealth.jsx:107 has the same admin_presence hole → "Admin Inactive (7d)" false-alarms every other gym. (Attention does it right via SECURITY DEFINER RPC.)
- [x] **P1-3 GymDiagnostic — the sales-closing artifact — overstates retention twice.** (a) No right-censoring: months a cohort hasn't lived yet count as survived → recent cohorts read ~100% (GymDiagnostic.jsx:105–108). (b) DATE columns parsed as UTC midnight → 8pm previous day in PR → joins/cancels dated the 1st (the most common billing date) land in the PREVIOUS month, shifting cohorts and seasonality (:65,72,74). Plus it's 100% hardcoded English — a Spanish-market sales tool that can't render Spanish.
- [x] **P1-4 GymDetail header stats are capped at 20.** fetchActivity limit(20) feeds "Sessions (30d)" and "Avg Sessions/Member" (GymDetail.jsx:357,370) and GymOverviewTab's Activity figure — a gym doing 400 sessions/mo reads "20". Wellness tab computes the same numbers correctly → same page contradicts itself.
- [x] **P1-5 "Monthly Income (12 months)" is synthetic back-projection** — today's prices × gyms-active-today over historical months; churned gyms show $0 for months they paid (PlatformAnalytics.jsx:160–171). "Retention Rate" hero = check-in rate, and = the "Active %" column (same number, two columns, different color scales); GymHealth's "Active %" uses a different source column (last_active_at) — same label, different metric across sibling pages. "Gyms with Rising Churn" measures level >50, not rise (velocity column exists, unused).
- [x] **P1-6 Member counts inflated everywhere outside Wellness**: GymsOverview + GymDetail + Settings "Total members" count ALL profiles (staff + imported_archived ghosts + null-gym leaks). The import flow this very page launches creates those ghosts.
- [x] **P1-7 GymHealth "Platform Health Trend" is unreachable dead code with a false promise** — single-point series → chart branch never renders; "Historical tracking begins from first refresh" but nothing persists snapshots (mv_gym_health_scores + refresh fn from 0281 exist server-side, zero readers).

## P2 — BROKEN ACTIONS & SILENT FAILURES

- [x] **P2-1 SupportConsole Reset Password dead cross-gym** — `admin_generate_password_reset` (0174) requires `p.gym_id = current_gym_id()` → works only on the founder's own gym; error copy "Member not found in your gym" actively misleads (SupportConsole.jsx:272).
- [x] **P2-2 SupportConsole Resend Invite dead on BOTH paths** — edge fn `admin-invite-member` doesn't exist; fallback writes `profiles.invite_token_regenerated_at`, a column in no migration (SupportConsole.jsx:292–323).
- [x] **P2-3 People-tab role dropdown bypasses every invariant** (GymDetail.jsx:378, GymPeopleTab.jsx:115): demote trainer ≠ `demote_trainer_atomically` (0358) → zombie trainer_clients; `additional_roles` untouched (0489 documents this exact bug); ROLE_OPTIONS lacks super_admin → founder's own row can one-click self-demote out of the platform tier; none of role/status/tier changes are audited.
- [x] **P2-4 Cards notifications invisible** — `notify_gym_card_delivery` + `print_cards_on_session_complete` (0463:744, ~875) insert with NULL audience → filtered out of every admin inbox, lands in the member filter. CardQueue's "Mark printed" heads-up has failed silently 100% of the time.
- [x] **P2-5 False all-clear states**: Attention renders the green "Every gym looks healthy" panel when its query FAILS (Attention.jsx:147–155, no error branch); CardQueue renders "Nothing to print" on failure, and a gyms-query failure silently skips ALL loaded cards (:158–160); SupportConsole search failures render "No results" (:124–147); GymHealth/PlatformAnalytics ignore { error } entirely → zeros that look real. Platform-wide pattern: only ErrorLogs/GymImport/GymOps/GymsOverview/PlatformSettings/SupportConsole have any failure UI.
- [x] **P2-6 Lifecycle feedback + integrity**: pause/reactivate/delete show no toast on failure (GymDetail.jsx:374–376, delete doesn't even log); delete→reactivate flips previously banned/frozen members to active (no pre_pause_status snapshot on delete); hard gym delete (0424) orphans organic members' auth.users (only import-shadow users wiped) → dead logins + reserved emails.
- [x] **P2-7 "Schedule deletion" never deletes** — nothing sweeps `pending_deletion` past `scheduled_deletion_at` (no cron/RPC/edge fn); the card's "permanently deleted on <date>" is fiction; lockout is real.
- [x] **P2-8 Rewards sub-tab reads the wrong table AND is RLS-dead** — reads reward_points (per-member balances; select_own-only since 0429) instead of gym_rewards → permanently "No rewards"; renders columns that don't exist (GymDetail.jsx:362, GymContentTab.jsx:302).
- [x] **P2-9 Platform inbox deep links dead for the founder** — all /admin/* routes bounce a primary super_admin back to /platform/attention (no view switcher exists in the platform tier; ViewSwitcherModal only mounts on pages that bounce him first). Also 3 producers (0412:302, 0445:78,177) link `/admin/members/<uuid>` — a route that doesn't exist even for gym admins (only /admin/members).
- [x] **P2-10 Audit misattribution + possible total loss** — log_admin_action (0299) stamps the ACTOR's gym, so support actions on gym X file under the founder's gym (gym filter never finds them); if the founder's profile has gym_id NULL, the NOT NULL constraint makes every platform logAdminAction throw inside fire-and-forget → rows lost entirely (needs a live check).
- [x] **P2-11 Support omnibox searches the wrong invite system** (gym_invites.token; admins hand out member_invites.invite_code) and can't search by email (profiles has no email column; no lookup RPC). Streak tile shows 0 cross-gym (streak_cache RLS gym-scoped, 0354:163).
- [x] **P2-12 Tier has two sources of truth** — detail dropdown writes subscription_tier; list badge reads plan_type first (0043 says plan_type is canonical); option sets disagree (lifetime unselectable in detail); change unaudited + error-swallowed.
- [x] **P2-13 Exercise editor enum drift** — UI offers Quads/Hamstrings/Cardio/Other (not in enums; save fails visibly) and omits Legs/Traps/Warm-Up (Legs exercises unfilterable, blank select on edit); "None" equipment → NOT NULL violation (PlatformSettings.jsx:49–57, 210).
- [x] **P2-14 Fake-success class on platform_config + video writes**: Operations executeToggleFeature, Settings toggleFeatureFlag/toggleEmailFlag, saveGymDefaults (green ✓ unconditional), template delete, and BOTH video-upload paths (exercise saves silently without video on upload failure).
- [x] **P2-15 AuditLog filter rot** — 3 options that can never match (gym_created/role_changed/moderate_post vs real create_gym/change_role/moderation); the platform's own actions (pause_gym, create_gym…) aren't selectable; ACTION_CONFIG maps ~10 never-produced actions while high-frequency real ones fall to the generic icon; "Active Actors" + ErrorLogs/Support summary tiles mix server counts with loaded-page-only counts (1000-row default caps).

## P3 — LOW / POLISH (grouped)

- [x] i18n: `platform.audit.*` namespace (142 keys) missing in BOTH locales (page is English even in EN-fallback terms); 16 `platform.support.*` keys ES-missing; `platform.gymHealth.issue.*` ES-missing; GymDetail families (contentTab.status, difficulty, challengeType, deleteEntity…) EN-only; `externalIdHelpPlain` missing in BOTH → raw key renders visibly (GymSettingsTab.jsx:197); session-expired modal keys missing both; GymOps/GymImport/GymDiagnostic 100% hardcoded English; `platform.ops.updatedPrefix` + `platformSettings.video` ES-missing.
- [x] ErrorLogs: `action_failed` dead filter (zero producers); Smartphone icon branch never fires (platform vs navigator.platform); "Open Operations" full-page reload via window.open(_self); no retention job on error_logs (grows forever); no acknowledge/resolve workflow; Attention→ErrorLogs link drops the gym context.
- [x] Settings Health tab: "15 edge functions deployed" = stale hardcoded array (real: 29); "Storage usage" = hardcoded bucket-name chips (one name wrong: progress-photos vs progress_photos); "Email Service: Active" = hardcoded prose; Your Account email always "—".
- [x] Incidents can never be acknowledged (onAcknowledge never passed); incident copy "up {N}x" shows a count, not a multiplier; storage probe measures reachability not health.
- [x] FeatureAdoption details: 5000-row unordered caps; "Gold = top quartile" is actually ≥75%-of-max; least-active excludes zero-activity gyms; dead tooltip formatter path; "Analytics" feature measures the cron, not the admin; "Rewards" reads referral_milestones not the rewards system.
- [x] GymHealth: 0-member gyms score Critical and drag the average (incl. inactive gyms — pulse RPC excludes them); TrendArrow renders level as motion; "Top Performer" formula undocumented/mislabeled.
- [x] GymDiagnostic: drops ALL size-1 cohorts (comment says only the latest); plan mortality also censoring-blind; avg tenure mixes censored+complete.
- [x] Dead code: GymsOverview AddMemberModal (~110 lines, never opened) + its icon imports; LOCAL_VIDEO_MAP; stats.activeUsers1h; unused profile/t destructures; Building2/Users imports; GymOps dynamic Tailwind class template-strings (renders by coincidence).
- [x] Misc: fulfillment toggle = unconfirmed one-tap flip (a11y: button-in-button); "Mark printed" possible without ever opening preview, no undo; status modal can't set banned (badge exists, no producer); membership_status_reason only appends; AddMemberModal minLength 6 vs validated 8; "At Risk" filter matches Moderate while KPI counts only At Risk; Overview sessions include non-completed (Wellness counts completed) → list vs detail disagree; recent-lookup chips re-search by name; double realtime channels on notifications rows; TYPE_META lacks admin_message.

## DUPLICATES / OVERLAP

1. **Three health formulas** (GymsOverview label, Wellness/0281 six-factor, GymHealth client) — list can say "Thriving" while the detail says 40.
2. **Three 30d-activity numbers per gym** (Detail header capped@20, Wellness completed-only, ActivityTrend RPC) — all on the same page family.
3. **Three meanings of "retention"** (Analytics=check-in rate, Diagnostic=cohort survival, admin tier=its own), and "Active %" differs between Analytics and GymHealth.
4. **Three admin-engagement definitions** (FeatureAdoption 30d days, GymHealth 7d binary — both RLS-dead; Attention RPC — correct).
5. Two feature-flag panels writing different keys for the same concepts (both no-ops); two health surfaces (Operations probes real, Settings Health inferior); AddMemberModal duplicated verbatim; pause/reactivate buttons in header + Settings tab; error visibility spread over 4 surfaces with inconsistent thresholds; two audit tables fed in parallel (0460-style inline inserts invisible to the platform page).

## GAPS (solo-operator)

No email lookup; no working cross-gym password reset or invite resend (the two most common support asks); no impersonation/"open as gym admin"; no owner set/repair UI; no platform invite management; no deletion executor (or honest copy); no import batch rollback (indexes exist, no RPC/UI); no error acknowledge + no retention; crash alerts in-app only (no push — 0517 never calls the push stack); no health-score history (the chart promises it); no real revenue ledger (billing events); no CSV exports on any platform page; no WoW/MoM deltas; GymDiagnostic not shareable/Spanish; no challenge status control; People tab unpaginated; GymsOverview pulls all profiles + all 30d sessions platform-wide for counts (perf).

## CLEAN BILLS

GymOps lifecycle page end-to-end (all 5 RPCs correct, every mutation error-handled); the import parse→commit path (incl. dedup, partial-failure handling, batch records); App Version Gate (real + audited); Attention page math/RPC/i18n; CardQueue print handoff chain; route/nav reachability (zero orphans); PlatformNotifications mechanics (optimistic + rollback, badge realtime in lockstep); AuditLog FK joins/pagination/date math; Operations health probes (real, not decorative).
