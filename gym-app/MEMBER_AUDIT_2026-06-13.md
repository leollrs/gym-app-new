# Member-Side Final Audit — 2026-06-13

Final pre-launch sweep. 18 functional areas, one Sonnet agent each, every finding adversarially verified against the real code by a second agent.

**111 raw findings → 71 confirmed, 40 refuted.**  Severity: 0 P0 · 6 P1 · 39 P2 · 26 P3.

---

## P1 — fix first (6)

- [ ] **`WarmUpTimer.onComplete` prop never invoked — warm-up and cool-down auto-advance is silently broken**  _( Active workout session (live set logging) )_
      - `gym-app/src/pages/ActiveSession.jsx:77-98`
      - Why: `WarmUpTimer` is the inline component used for both warm-up exercises and cool-down stretches. When the countdown reaches 0 the RAF tick sets `done = true` and fires a vibration, but `onComplete()` — the callback that advances to the next warm-up exercise or exits the warm-up/cool-down phase — is never called. `onComplete` appears in the `useEffect` dependency array (line 98) which proves the intent, but the call itself is missing from the `if (remaining <= 0)` branch. The user must tap a button to advance; the timer never auto-progresses.
      - Fix: The proposed fix is correct: add `onComplete?.()` after `setDone(true)` in the zero-remaining branch. However, note that WarmUpTimer has no "Next" button in its own JSX — the manual path comes from the parent's sticky footer, not from inside the timer component, so the fix description was slightly imprecise but the code change itself is right.

- [ ] **GPS-denied: timer permanently frozen at 00:00:00 for GPS activity types**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/pages/LiveCardio.jsx:190`, `gym-app/src/pages/LiveCardio.jsx:222-241`, `gym-app/src/pages/LiveCardio.jsx:431`, `gym-app/src/pages/LiveCardio.jsx:462-477`
      - Why: When a user selects a GPS-eligible activity (Running, Walking, Cycling, Hiking, Skiing) and denies location permission, the GPS tracker is created but `tracker.start()` is never called. The timer interval (lines 222-241) skips immediately because `if (useGps) return;` on line 223. The gpsTickSec update interval (lines 379-388) reads `trackerRef.current.snapshot()` which returns `elapsedSec: 0` because `state.startedAt` is null (set only inside `start()`). Result: the big displayed timer stays at 00:00:00 for the entire session even though the user is actively tracking time. The error banner says 'GPS unavailable — tracking time only' but time never actually ticks.
      - Fix: When GPS is denied (lines 466-469), additionally reset `useGps` to false for the current session. Since `useGps` is derived from `selectedType.gps`, the cleanest fix is to add a `[gpsOverridden, setGpsOverridden]` state (default false) and set it to true on GPS denial. Then: `const useGps = !!selectedType.gps && !gpsOverridden;`. This lets the timer interval fire and the session proceeds as a timed-only activity, consistent with the error message shown.

- [ ] **AchievementToast renders all hardcoded achievement icons as literal text strings**  _( Goals & achievements )_
      - `gym-app/src/components/AchievementToast.jsx:131-133`, `gym-app/src/lib/achievements.js:7-218`
      - Why: The achievement celebration overlay — the most prominent gamification moment — displays the icon name text (e.g. 'Dumbbell', 'Flame', 'Trophy') at 44px font size instead of the actual lucide icon. Every one of the 30+ hardcoded ACHIEVEMENT_DEFS entries is broken this way. Only gym-admin-created custom achievements (which use emoji strings) render correctly.
      - Fix: Import the same set of lucide icons used in Profile.jsx and add an ICON_MAP inside AchievementToast. Then replace line 132 with a conditional: if `current.icon` matches a lucide name in ICON_MAP, render `<IconComponent size={44} style={{ color: current.color }} />`, otherwise render it as a text span (for emoji from custom achievements). The ICON_MAP from Profile.jsx already covers all 30+ names in ACHIEVEMENT_DEFS.

- [ ] **Milestone feed bypasses anonymization — real names of strangers visible**  _( Leaderboard )_
      - `gym-app/src/pages/Leaderboard.jsx:890-914`
      - Why: The entire leaderboard section applies a carefully designed anonymize() function that replaces names/avatars of non-friends with 'Member'/null. However, the inline 'Recent Highlights' milestone feed at lines 890-914 renders milestones.data directly without passing it through anonymize(). entry.name (line 904) shows the real full_name of any gym member who hit a milestone, regardless of whether the viewer is friends with them. The get_milestone_feed RPC returns profile_id in each row (confirmed in migration 0143), so the fix is straightforward, but it is not applied.
      - Fix: The proposed fix is directionally correct but incomplete. The `get_milestone_feed` RPC returns `me.id` (milestone_events PK) as the `id` field, not the profile UUID — so calling `anonymize(milestones.data)` with the current `anonymize` function (which checks `e.id === uid || friendIds.has(e.id)`) would incorrectly anonymize everyone including the viewer. Two options: (1) In the SQL, change `me.id` to `me.profile_id AS id` and add `me.id AS event_id` for the map key — then `anonymize(milestones.data)` works correctly and replace `key={entry.id}` with `key={entry.event_id}`. (2) Alternatively, keep the data as-is and in the JSX check `entry.profile_id === uid || friendIds.has(entry.profile_id)` inline before rendering `entry.name`, showing `t('leaderboard.anonMember', 'Member')` for non-friends. Option 1 is cleaner and consistent with the rest of the board.

- [ ] **"Save" button silently writes to app sandbox (Directory.Documents), image never reaches Photos**  _( Share-card system (JUST REWORKED — scrutinize hard) )_
      - `gym-app/src/components/share/ShareSheet.jsx:677`, `gym-app/src/components/share/ShareCardioSheet.jsx:408`, `gym-app/src/components/share/ShareAchievementSheet.jsx:438`, `gym-app/src/components/share/ShareMonthSheet.jsx:118`
      - Why: Every share sheet's `dest === 'save'` branch uses Capacitor `Filesystem.writeFile` with `Directory.Documents`. This writes into the app's private sandbox — the write succeeds without any Photos permission, so the `catch` fallback to `shareBlob()` never fires. The user taps Save, sees a success toast, and the image simply disappears into a directory they can never access. The correct save-to-Photos pathway (used correctly elsewhere in `ShareCardRenderer.jsx:shareBlob()` and `saveBlob.js`) is: write to `Directory.Cache`, then call `Share.share({ files: [uri] })` which presents the native share sheet where the user chooses 'Save Image'.
      - Fix: Replace the entire `dest === 'save'` try/catch block in all four sheets with a single call to the existing `shareBlob(blob, filename)` helper (already imported in most sheets). This correctly uses `Directory.Cache` + `Share.share({files:[uri]})` and is the pattern already proven correct in `ShareCardRenderer` and `saveBlob.js`.

- [ ] **FriendDuelsSection and TeamFormationModal join `profiles` directly — violates member RLS, opponent names always null**  _( Rewards, challenges, referrals, QR/scan claim )_
      - `gym-app/src/pages/Challenges.jsx:855`, `gym-app/src/pages/Challenges.jsx:1867`
      - Why: The FriendDuelsSection (line 1867) fetches friend duel opponent name/avatar by joining `profiles` directly via foreign-key alias. The TeamFormationModal friend-picker (line 855) does the same via `friendships`. The file's own comment block at lines 72-81 explicitly documents that member RLS (migration 0289) lets a regular member read ONLY their own profiles row — so both joins silently return null for every non-self participant. Duel cards render with no opponent name or avatar; the friend-picker in TeamFormationModal shows blank entries. Both features appear completely broken for real member accounts (admins testing in member view see their own data and do not notice).
      - Fix: Replace both direct `profiles` joins with the two-step pattern already used in this file: collect the profile IDs from the query result, then call `fetchMemberProfiles(ids)` (defined at line 82) which queries `gym_member_profiles_safe`. For FriendDuelsSection: after `setDuels(data || [])`, extract unique `[challenger_id, challenged_id]` IDs, call `fetchMemberProfiles`, and merge the results into duel objects. For TeamFormationModal friendships: collect `requester_id`/`addressee_id`, resolve via `fetchMemberProfiles`, and map names/avatars from the result map.

## P2 (39)

- [ ] [i18n] **Two deep-link toast messages hardcoded in English — never translated**  _( Auth, onboarding & deep links )_
      - `gym-app/src/App.jsx:1491`, `gym-app/src/App.jsx:1510`
      - When an already-authenticated user opens an /invite/:code or /referral/:code deep link, they see a toast in hardcoded English regardless of the app's language setting. The App component uses useTranslation('common') but these two calls bypass t().
      - Fix: Add two keys to both locale files (e.g. common.inviteForNewAccounts / common.referralForNewSignups), then wrap: showToast(t('inviteForNewAccounts'), 'info') and showToast(t('referralForNewSignups'), 'info').

- [ ] [i18n] **StuckLoadingRecovery shows hardcoded Spanish text to all users regardless of language**  _( Dashboard & app shell )_
      - `gym-app/src/components/StuckLoadingRecovery.jsx:205`, `gym-app/src/components/StuckLoadingRecovery.jsx:208`, `gym-app/src/components/StuckLoadingRecovery.jsx:266`, `gym-app/src/components/StuckLoadingRecovery.jsx:269`, `gym-app/src/components/StuckLoadingRecovery.jsx:293`, `gym-app/src/components/StuckLoadingRecovery.jsx:298`
      - All visible strings in StuckLoadingRecovery.jsx are hardcoded Spanish. An English-configured gym's members see 'Reiniciando la app…', 'Limpiando la caché y volviendo a cargar. Tu sesión se mantiene.', '¿La app se quedó cargando?', 'Limpiando…', 'Restablecer', and aria-label='Cerrar' — regardless of the i18n locale setting. This component is specifically shown during a degraded recovery state, which is exactly when users need clear, language-appropriate messaging.
      - Fix: Add `const { t } = useTranslation('pages');` and replace the six hardcoded strings with t() calls. Add corresponding keys to both en/pages.json and es/pages.json under a new `stuckLoading` namespace (e.g., `stuckLoading.resetting`, `stuckLoading.cacheClearing`, `stuckLoading.heading`, `stuckLoading.body`, `stuckLoading.reset`, `stuckLoading.close`). The rootIsEmpty() regex at line 72 already handles both 'loading' and 'cargando' intentionally for detection — leave that unchanged.

- [ ] [broken-flow] **GenerateWorkoutModal saves no schedule_map with routine_ids — active-routine delete protection, A/B week display, and program name all silently break**  _( Workout generation & routines )_
      - `gym-app/src/components/GenerateWorkoutModal.jsx:665-675`, `gym-app/src/pages/Workouts.jsx:803-818`, `gym-app/src/pages/Workouts.jsx:914-920`
      - GenerateWorkoutModal.handleGenerate inserts a generated_programs row with no schedule_map column. The Workouts page reads schedule_map for three critical things: (1) activeProgramRoutineIds (lines 803-818) — if routine_ids/routine_ids_a/routine_ids_b are all absent the Set is empty and falls back to checking name.startsWith('Auto:'), which means ALL old 'Auto:' routines from prior programs also appear protected; (2) the A/B weekly variant display (lines 914-920) — hasVariants is false, so getRoutinesForWeek falls back to the DOW→workoutScheduleMap path (only variant A ever shows); (3) gpName (line 394) — no display_name means the program hero shows the raw split_type like 'Upper Lower' instead of a creative name. In contrast, personalProgramService.generateAndSavePersonalProgram (the onboarding/regenerate path) correctly writes a full schedule_map. This path is used when the user generates from the Workouts page modal, not from onboarding.
      - Fix: The proposed fix is correct in intent. After saving routines in handleGenerate, construct a schedule_map matching personalProgramService's structure and include it in the generated_programs insert. At minimum: routine_ids (savedRoutineAIds.concat(savedRoutineBIds)), routine_ids_a (savedRoutineAIds), routine_ids_b (savedRoutineBIds), display_name (generated creative name), start_dow (new Date().getDay()), normal_dows (scheduleDays). This closes all three regressions at once. No schema change needed — schedule_map is already a JSONB column on generated_programs (personalProgramService already writes it successfully).

- [ ] [broken-flow] **adaptiveWorkout.js queries a non-existent 'sets' table — Workout of the Day always generates with zero training history**  _( Workout generation & routines )_
      - `gym-app/src/lib/adaptiveWorkout.js:475-488`
      - generateAdaptiveWorkout fetches exercise history by querying `.from('sets').select('exercise_id, session_id')`. No table named 'sets' exists — the real tables are session_exercises and session_sets (confirmed in migration 0001_initial_schema.sql, line 366). The query silently returns data:null (PostgREST 404 is swallowed), so recentSets is always []. The recovery analysis therefore treats every muscle as 100% recovered and frequency-debt boosted (daysSinceLast === null path), producing a random-but-biased workout every time with no real personalization from actual training history. Users see a 'personalized' WOD that ignores everything they just trained.
      - Fix: The fix is simpler than proposed: `session_exercises` already has both `exercise_id` and `session_id` as direct columns (confirmed in migration 0001 lines 354-360). Replace the broken query with `.from('session_exercises').select('exercise_id, session_id').in('session_id', sessionIds)`. The `completed_at` attachment from `sessionMap` in the existing code at line 485-488 works correctly as-is after this change. No need for the nested `session_sets` join since the recovery analysis only needs `exercise_id` + `completed_at` (derived from session), not weight/reps data.

- [ ] [broken-flow] **getTodayProgramRoutineIds in useRoutines uses name.startsWith('Auto:') heuristic — misclassifies after creative-name migration, wrong 'today' routines surfaced**  _( Workout generation & routines )_
      - `gym-app/src/hooks/useRoutines.js:12-36`
      - getTodayProgramRoutineIds (called after every fetchRoutines to sync to Apple Watch) identifies program routines by `r.name?.startsWith('Auto:')`. This was correct for old-style 'Auto: Upper A' names. Since the creative-name migration, new programs store routines as 'Auto: Apex Build', 'Auto: Iron Frame', etc. — these still match startsWith('Auto:') so that part survives. However the secondary filter `r.name.endsWith(' A') || !r.name.endsWith(' B')` for Week A and `r.name.endsWith(' B')` for Week B no longer works: creative names never end in ' A' or ' B'. For Week A the fallback `!r.name.endsWith(' B')` returns true for ALL routines (including Week B routines since 'Auto: Pillar Day' does not end in ' B'), so the full routine set is treated as Week A every time. Week B detection returns an empty set every even week.
      - Fix: The proposed fix direction is correct: fetch `schedule_map` in the query (add it to `.select('program_start, expires_at, schedule_map')`) and use `new Set(program.schedule_map?.routine_ids_a || [])` for Week A and `new Set(program.schedule_map?.routine_ids_b || [])` for Week B, then filter `routines` whose id is in that set. The same fix is needed in main.jsx line 282 where the heuristic is duplicated.

- [ ] [crash] **handleGenerate crash if result is null — unguarded result.routinesA.length throws**  _( Workout generation & routines )_
      - `gym-app/src/components/GenerateWorkoutModal.jsx:604-605`
      - In handleGenerate (called when user clicks 'Generar mi programa'), `result` may be null if the generateProgram() call in the step-1 effect threw and was caught (logged but setResult not called), or if the user somehow triggers Generate before the effect runs. Line 604: `const trainingDays = result.routinesA.length;` — accessing .routinesA on null throws TypeError and causes an unhandled exception, which because the try/catch wraps the whole function, will be caught and show the error message. But the try/catch saves it from a full crash to just an opaque error toast, so it's P2 not P0.
      - Fix: Add `if (!result) { setError(t('generateWorkout.generating')); setSaving(false); return; }` at the top of handleGenerate.

- [ ] [broken-flow] **reactivatePersonalProgram passes wrong gym_id — uses sourceProgram.gym_id but the gym may have changed since the program was created**  _( Workout generation & routines )_
      - `gym-app/src/lib/personalProgramService.js:462-475`
      - reactivatePersonalProgram inserts the new generated_programs row with `gym_id: sourceProgram.gym_id`. If the member transferred to a different gym, their profile.gym_id now differs, and the new program's gym_id points to their old gym. All subsequent RLS queries that filter by gym_id will misfire — workout_schedule inserts at line 488 also use sourceProgram.gym_id. In practice, gym transfers are rare, but the code should use the user's current profile.gym_id. This could also prevent the new program from appearing in gym analytics.
      - Fix: The proposed fix (fetch profiles.gym_id at function start and use that) is correct, but the reason it is needed is not gym transfers — it is that gym_id is never included in the generated_programs select query that produces sourceProgram. The fix should either: (a) add `gym_id` to the generated_programs select field list in Workouts.jsx (lines 642, 1067, 3332) so sourceProgram carries it, or (b) fetch `profiles.gym_id` at the start of reactivatePersonalProgram the same way regenerateMemberProgram does at line 298-300. Option (b) is safer as it uses the current profile value and does not require coordinating three call sites.

- [ ] [crash] **`logger` used but never imported — ReferenceError on any network error**  _( Active workout session (live set logging) )_
      - `gym-app/src/pages/ActiveSession.jsx:702`, `gym-app/src/pages/ActiveSession.jsx:724`, `gym-app/src/pages/ActiveSession.jsx:738`
      - `logger.warn(...)` is called three times in `ActiveSession.jsx` — once in the realtime `session_cues` subscription and twice in the background set-log backfill effect — but `logger` is never imported. The module at `src/lib/logger.js` exists and exports a default, but there is no `import logger from '../lib/logger'` anywhere in the file. Every time `ack_session_cue` or the backfill query returns an error (network drop, RLS rejection, cold-start race), the error handler itself throws `ReferenceError: logger is not defined`, which is uncaught inside a `.then()` callback and will crash the active session page.
      - Fix: Add `import logger from '../lib/logger';` to the import block at the top of `ActiveSession.jsx` (alongside the other `../lib/*` imports).

- [ ] [race] **Apple Watch `complete_set` handler captures stale `adjustedRestSeconds` — wrong rest duration after Watch-triggered set**  _( Active workout session (live set logging) )_
      - `gym-app/src/pages/ActiveSession.jsx:968`, `gym-app/src/pages/ActiveSession.jsx:1076`
      - The `onWatchMessage` handler for the `complete_set` case calls `handleToggleComplete(curEx.id, idx, exName(curEx), adjustedRestSeconds ?? curEx.restSeconds ?? 90)` (line 968), where `adjustedRestSeconds` is the state value set when the user taps ±15 s on the rest-timer adjust buttons. The effect's dependency array is `[exercises, currentExerciseIndex, loggedSets]` (line 1076); `adjustedRestSeconds` is not included. If the user taps +15 s or −15 s before completing a set from their Watch, the closure captures the value of `adjustedRestSeconds` from the previous render and the wrong rest period is started.
      - Fix: Add `adjustedRestSeconds` to the effect dependency array:
```js
}, [exercises, currentExerciseIndex, loggedSets, adjustedRestSeconds]);
```
Alternatively, store `adjustedRestSeconds` in a ref that is updated on every render and read the ref inside the handler.

- [ ] [broken-flow] **`restNotificationScheduled` flag not reset on natural rest expiry — second and subsequent rest notifications are silently skipped**  _( Active workout session (live set logging) )_
      - `gym-app/src/pages/ActiveSession.jsx:2090-2097`
      - The rest-timer effect schedules a Capacitor/Web OS notification (`scheduleRestDoneNotification`) the first time `isResting` becomes true and guards it with `restNotificationScheduled.current`. The flag is reset to `false` in the skip-rest path and inside `handleToggleComplete` when a new rest period begins. However, in the natural-expiry branch (when `restTimer` reaches 0) the flag is never reset. After the first rest period completes naturally, `restNotificationScheduled.current` stays `true` for the rest of the session, so no subsequent rest-done notification is ever scheduled.
      - Fix: Add the flag reset inside the natural-expiry branch before the `return`:
```js
if (restTimer <= 0) {
  setIsResting(false);
  restStartedAt.current = null;
  restNotificationScheduled.current = false; // allow next rest to schedule
  try { localStorage.removeItem(restStateKey); } catch { }
  try { navigator.vibrate?.([200, 100, 200, 100, 200]); } catch { }
  window.scrollTo(0, 0);
  return;
}
```

- [ ] [data-loss] **`sessionRating` collected in UI but omitted from `complete_workout_v2` payload — rating always lost**  _( Active workout session (live set logging) )_
      - `gym-app/src/pages/ActiveSession.jsx:2736-2778`, `gym-app/src/pages/active-session/SessionSummary.jsx:56-68`
      - `SessionSummary` renders a 5-emoji rating picker and calls `onRatingChange(value)` when the user taps an emoji. The parent stores the selection in `sessionRating` state (line 814 in `ActiveSession.jsx`) and passes it back to `SessionSummary` as the `sessionRating` prop (confirmed in `SessionSummary.jsx` line 16). However, when `handleFinish` builds the `complete_workout_v2` RPC payload (lines 2736–2778), `session_rating: sessionRating` is absent from the payload object. The user's post-workout feeling is silently dropped every time.
      - Fix: The proposed fix is incomplete. Three changes are required: (1) A DB migration adding `session_rating SMALLINT CHECK (session_rating BETWEEN 1 AND 5)` to `workout_sessions`. (2) An update to `complete_workout` (or `complete_workout_v2`) to read `(p_payload->>'session_rating')::SMALLINT` and write it into the `workout_sessions` INSERT/UPDATE. (3) Adding `session_rating: sessionRating ?? null` to the client payload object in `handleFinish` (ActiveSession.jsx around line 2777). Without steps 1 and 2, adding the field to the client payload changes nothing — the RPC ignores unknown payload fields.

- [ ] [data-loss] **Share card displays wrong pace (1.6× too slow) for mile-unit GPS runs before saving**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/pages/LiveCardio.jsx:1321`, `gym-app/src/components/share/ShareTplCardio.jsx:39-42`
      - In the 'done' phase (run ended but not yet logged), LiveCardio passes `avgPaceSecPerKm: gpsState?.avgPaceSecPerUnit` to ShareCardioSheet. When the user's unit is 'mi', `avgPaceSecPerUnit` is in sec/mi. But `paceDisplay()` in ShareTplCardio treats the input as sec/km and multiplies by 1.60934 to convert to sec/mi for display. This double-counts the conversion: a 10:00/mi pace (600 sec/mi from tracker) becomes 600 × 1.60934 = 965 sec/mi displayed as 16:05/mi. The correct conversion happens in `handleSubmit` (line 551-556) where sec/mi is divided by 1.60934 before DB storage. The bug only appears in the 'done' phase share before 'Finish & Log'; saved sessions are correct.
      - Fix: In LiveCardio line 1321, mirror the conversion from handleSubmit: `avgPaceSecPerKm: gpsState?.avgPaceSecPerUnit != null ? (unit === 'mi' ? gpsState.avgPaceSecPerUnit / 1.60934 : gpsState.avgPaceSecPerUnit) : null`

- [ ] [other] **Share card always shows teal accent color, ignoring gym's custom brand color**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/pages/LiveCardio.jsx:1300-1328`, `gym-app/src/pages/CardioSessionDetail.jsx:327-343`, `gym-app/src/components/share/ShareCardioSheet.jsx:171`
      - Neither LiveCardio nor CardioSessionDetail pass an `accent` prop to ShareCardioSheet. ShareCardioSheet defaults to '#2EC4C4' (teal). This means every gym's share card — regardless of their configured brand color — shows a teal accent. For a white-label product where gyms pay to have their own branding, their members' share cards won't reflect the gym's colors. LiveCardio does define `const accent = 'var(--color-accent, #2EC4C4)'` at line 757 but does not pass it as a prop to ShareCardioSheet.
      - Fix: In both LiveCardio and CardioSessionDetail, resolve the gym accent at render time and pass it: `accent={getComputedStyle(document.documentElement).getPropertyValue('--color-accent').trim() || '#2EC4C4'}` as an explicit prop to ShareCardioSheet. This is the same pattern CardioSessionDetail already uses for StaticRouteMapImage at line 237.

- [ ] [broken-flow] **GPS pre-render cache poisoned with broken route color when Mapbox unavailable and CSS var is passed to canvas**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/pages/LiveCardio.jsx:678-684`, `gym-app/src/lib/staticRouteMap.js:66`, `gym-app/src/lib/staticRouteMap.js:129`
      - LiveCardio calls `prerenderAndCache({ ..., accent })` where `accent = 'var(--color-accent, #2EC4C4)'` (a CSS variable expression string, not a hex). The renderRouteMap chain's CartoDB tile-stitcher (`generateStaticRouteMap`) and route-only fallback (`generateRouteOnlyImage`) pass `opts.accent` directly to `ctx.strokeStyle`. The HTML Canvas 2D API does not resolve CSS custom properties — it silently ignores the invalid color string and uses the previously set strokeStyle (the white halo color `rgba(255,255,255,0.95)`). The result is a cached PNG where the route polyline is white. When the share sheet opens and hits the IndexedDB cache, it shows a white route on a light map background — essentially invisible. Mapbox (the primary path) correctly resolves CSS vars via `resolveHexColor()`, so this only affects sessions where Mapbox fails or is unreachable.
      - Fix: In staticRouteMap.js, add a helper to resolve the accent color before use — the same `resolveHexColor()` function already in mapboxStatic.js. Or, when calling `prerenderAndCache` in LiveCardio, resolve the CSS var first: `const resolvedAccent = getComputedStyle(document.documentElement).getPropertyValue('--color-accent').trim() || '#2EC4C4';`

- [ ] [data-loss] **MonthlyProgressReport shows body measurements in cm while ProgressBody shows them in inches**  _( Progress, metrics, PRs, charts )_
      - `gym-app/src/components/MonthlyProgressReport.jsx:765`
      - The Monthly Progress Report displays circumference measurements (chest, waist, hips, arms, thighs) in raw centimeters (as stored in the DB), while the Body tab in ProgressBody converts them to inches before display. A user logging 35 inches for their chest sees '35.0 in' on the Body tab, then opens the Monthly Report and sees '88.9cm'. Same data, conflicting units — looks like a reporting error to the user.
      - Fix: The proposed fix is correct. In MonthlyProgressReport.jsx around line 759–774: import `cmToIn` from `../pages/progress/progressConstants` (or wherever it resolves), change `const unit = key === 'body_fat_pct' ? '%' : 'cm'` to `const unit = key === 'body_fat_pct' ? '%' : 'in'`, and convert the raw values before use: `const rawS = s; const rawE = e; const dispS = (key !== 'body_fat_pct' && rawS != null) ? cmToIn(rawS) : rawS; const dispE = (key !== 'body_fat_pct' && rawE != null) ? cmToIn(rawE) : rawE;` then use `dispS`/`dispE` for both the display value and the diff calculation.

- [ ] [broken-flow] **MonthlyProgressReport fires N individual createSignedUrl calls (one per progress photo)**  _( Progress, metrics, PRs, charts )_
      - `gym-app/src/components/MonthlyProgressReport.jsx:306-312`
      - When the Monthly Report renders progress photos, it calls `createSignedUrl` individually for each photo inside `Promise.all`. For a user with 10+ photos in a month, this fires 10+ storage API requests in parallel. The ProgressBody.jsx was specifically refactored to use the batch `createSignedUrls` (plural) to fix this exact pattern. If Supabase rate-limits or any individual call fails, the affected photo gets an empty URL and renders as a broken Camera placeholder — the user's before/after comparison is silently broken.
      - Fix: Replace the per-photo loop with a single batch call: `const paths = (photos ?? []).map(p => p.storage_path); const { data: signed } = await supabase.storage.from('progress-photos').createSignedUrls(paths, 3600); const urlByPath = {}; (signed ?? []).forEach(s => { if (s.signedUrl) urlByPath[s.path] = s.signedUrl; }); const photosWithUrls = (photos ?? []).map(p => ({ ...p, url: urlByPath[p.storage_path] ?? '' }));`

- [ ] [data-loss] **Body-weight / body-fat goals show 100% progress bar from day 1 when start_value is null**  _( Goals & achievements )_
      - `gym-app/src/components/GoalsSection.jsx:147-159`, `gym-app/src/components/GoalsSection.jsx:1112-1137`
      - The direction-aware `goalProgressPct` function has a correct formula that requires `start_value` to be populated. When `start_value` is null/NaN (which applies to any goal created before migration 0557 is applied, or goals created when no body metric has ever been logged), the legacy fallback `current / target * 100` is used. For decrease goals (body_weight or body_fat where current > target), this formula returns values above 1.0, which are clamped to 100%. A member wanting to drop from 200 lbs to 170 lbs sees 100% progress immediately and every day until they actually hit 170 lbs. Migration 0557 (which adds the `start_value` column) is confirmed PENDING APPLY per the project memory file.
      - Fix: Apply migration 0557 to add the `start_value` column. Existing body_weight / body_fat goals with `start_value = null` will be fixed in two ways: (1) `updateBodyMetricGoals` already backfills `start_value` on the first body-metric log after migration (goalUpdater.js lines 133-137), and (2) when the user opens the GoalModal to create a new goal of the same type, the upsert will seed it. No additional code change needed once the migration is applied.

- [ ] [broken-flow] **Blocking a user does not hide their existing comments from feed threads**  _( Social feed, community, messages, moderation )_
      - `gym-app/src/pages/SocialFeed.jsx:593-619`, `gym-app/src/pages/SocialFeed.jsx:1436-1438`
      - When a user blocks someone, the feed correctly filters out that person's posts via the client-side blockedUsers Set (visibleFeed filter at line 1437). However, the loadComments() function inside FeedCard fetches all feed_comments for a post without any blocked-user filter. The blockedUsers state lives in the parent SocialFeed component and is never passed down to FeedCard. Additionally, there is no server-side RLS SELECT filter on feed_comments for blocked relationships (only INSERT is blocked via is_blocked()). As a result, a blocked user's existing comments on other members' posts remain visible when the viewer opens the comments section, contradicting the user's expectation that blocking removes all contact.
      - Fix: The simplest fix does not require passing the parent blockedUsers prop down. FeedCard already loads its own `hiddenIds` (bidirectional blocked set) lazily when comments open (lines 529-544). Change the comments render at line 855 from `comments.map(c => <CommentRow key={c.id} comment={c} />)` to `comments.filter(c => !hiddenIds.has(c.profile_id)).map(c => <CommentRow key={c.id} comment={c} />)`. Since `hiddenIds` is populated in the same `showComments` effect that triggers `loadComments()`, both will be ready by render time.

- [ ] [i18n] **ReportContentModal shows raw snake_case key names for 4 of 8 report reason options in all locales**  _( Social feed, community, messages, moderation )_
      - `gym-app/src/components/ReportContentModal.jsx:176-217`, `gym-app/src/lib/moderation.js:25-34`, `gym-app/src/i18n/locales/en/pages.json`, `gym-app/src/i18n/locales/es/pages.json`
      - ReportContentModal imports REPORT_REASONS from moderation.js (8 reasons: spam, harassment, hate_speech, nudity, violence, dangerous, inappropriate, other) and renders each with `t('moderation.report.reasons.${reason}', { defaultValue: reason })`. The entire moderation.report.reasons namespace does not exist in either pages.json locale. The t() call falls back to defaultValue which is the raw reason key itself. Users see clickable buttons labeled 'hate_speech', 'nudity', 'violence', 'dangerous' instead of human-readable labels — both in English and Spanish. The inline SocialFeed report modal uses a different key namespace (social.report.reasons) which only has 4 translations (spam, inappropriate, harassment, other) and those work correctly. Only the shared ReportContentModal (used from ProfilePreview and ContentActionMenu) is broken.
      - Fix: Add the moderation.report.reasons object to both EN and ES pages.json with all 8 labels: { spam, harassment, hate_speech, nudity, violence, dangerous, inappropriate, other }. EN: hate_speech='Hate speech', nudity='Nudity or sexual content', violence='Violence or harm', dangerous='Dangerous or illegal'. ES equivalents: discurso_de_odio, contenido sexual/desnudez, violencia, peligroso/ilegal.

- [ ] [broken-flow] **Streak leaderboard falls back to own-row-only read if migration 0524 not applied**  _( Leaderboard )_
      - `gym-app/src/hooks/useSupabaseQuery.js:191-209`, `gym-app/src/pages/Leaderboard.jsx:621`, `gym-app/src/pages/Leaderboard.jsx:701`
      - Migration 0524 (get_leaderboard_streaks RPC) is marked 'APPLY MANUALLY' and may not be deployed. When the RPC does not exist, the hook falls back to a direct streak_cache table read at line 202-208. Migration 0354 (already applied per memory) restricts streak_cache SELECT to members' own rows only. So in the fallback path, every member sees only themselves on the streak board — a one-person leaderboard. The streak CategoryCard will show a single entry (themselves), and the expanded modal for Streak will also show only one row. No error is thrown; the board just silently shows wrong/incomplete data.
      - Fix: Apply migration 0524_get_leaderboard_streaks.sql to the production database. The RPC is SECURITY DEFINER and bypasses RLS correctly. The fallback in useSupabaseQuery.js is intentional as a graceful degradation, but 0524 must be live for the streak board to function for members.

- [ ] [broken-flow] **ShareCardioSheet sends raw prop `accent` to IG Story background, ignoring user's custom color**  _( Share-card system (JUST REWORKED — scrutinize hard) )_
      - `gym-app/src/components/share/ShareCardioSheet.jsx:450`
      - In ShareCardioSheet, the IG Story call at line 450 passes `backgroundTopColor: accent` — the raw prop from the gym's brand color. A `customAccent` state variable exists for user-selected colors (mirroring ShareSheet), but it is never applied to the IG Story call. After the user customizes the accent color via the color picker, the IG Story pasteboard receives the original gym brand color instead of the user's selection.
      - Fix: Change `backgroundTopColor: accent` to `backgroundTopColor: customAccent || accent` on both branches of the ternary (matching the pattern in ShareSheet.jsx).

- [ ] [broken-flow] **composedBlob (always 1080x1920 story ratio) is sent to IG Feed destination, producing wrong aspect ratio**  _( Share-card system (JUST REWORKED — scrutinize hard) )_
      - `gym-app/src/components/share/ShareSheet.jsx:650`
      - When the user uses StickerComposer to place the card on a background photo, `compose()` always outputs a 1080x1920 canvas (the story canvas). `buildCard()` short-circuits and returns `composedBlob` regardless of the selected format. If the user then selects 'IG Feed' (square 1080x1080) and taps Share, the 9:16 story image is sent to Instagram Feed, which crops or pillarboxes it. The code comment says 'composedBlob is format-independent' but that is incorrect for non-story destinations.
      - Fix: When `composedBlob` is set and the destination is anything other than `ig-story`, either: (a) disable the non-story share buttons with a tooltip explaining the composed image is story-format only, OR (b) re-composite `composedBlob` at the selected format's dimensions inside `buildCard()` before returning. Option (a) is simpler and avoids a second canvas operation.

- [ ] [data-loss] **TargetEditModal auto-calculates macros from hardcoded BMR=1700 instead of real user metrics**  _( Nutrition & meal plans )_
      - `gym-app/src/pages/Nutrition.jsx:1641-1648`
      - The macro target auto-calculation in `TargetEditModal` uses a hardcoded `bmr = 1700` instead of calling the existing `calculateMacros` utility (which uses Mifflin-St Jeor with the user's real weight, height, age, and sex). Every member gets the same auto-suggested targets regardless of body composition — a 120lb woman and a 250lb man will both see TDEE ~2635 kcal at 'moderate' activity. The `calculateMacros` function in `macroCalculator.js` already implements the correct formula and is available in scope.
      - Fix: The `onAutoCalculate` prop is already correctly implemented in the parent (`handleAutoCalculate` calls `calculateMacros` with real onboarding metrics). The fix is entirely inside `TargetEditModal`: replace the hardcoded `const bmr = 1700` block and its derived `tdee/autoKcal/autoProtein/autoFat/autoCarbs` variables with a call to `onAutoCalculate()` inside the `useEffect`. Since `onAutoCalculate` already calls `setDraft` with real values, the effect body can simply become `if (open) onAutoCalculate?.()`. Remove lines 1641-1648 and update the `useEffect` accordingly. No prop changes needed — `onAutoCalculate` is already passed at both call sites.

- [ ] [data-loss] **NotificationSettings: DB write failures are silently swallowed — user believes prefs saved but they are not**  _( Profile, settings, account, photos )_
      - `gym-app/src/pages/NotificationSettings.jsx:84-108`
      - Both updatePref and updateGroup use supabase-js v2 which never throws — it returns {data, error}. The functions check no error field and have no catch block. If the Supabase UPDATE fails (RLS rejection, network timeout, wrong column name), the optimistic toggle stays flipped but the preference is never persisted. The user leaves thinking they disabled push notifications, but the DB still has the old value and push will keep firing.
      - Fix: Destructure the error: `const { error } = await supabase…update(…)`. If error is set, revert the optimistic state and show a toast. Mirror the pattern already used for leaderboard_visible in MemberSettings.jsx (lines 374-382).

- [ ] [i18n] **validateImage.js error strings are hardcoded English — shown verbatim to Spanish-locale members**  _( Profile, settings, account, photos )_
      - `gym-app/src/lib/validateImage.js:14`, `gym-app/src/lib/validateImage.js:30`, `gym-app/src/lib/validateImage.js:35`, `gym-app/src/lib/validateImage.js:43`, `gym-app/src/pages/Profile.jsx:679`
      - validateImageFile() returns raw English strings ('Image must be under 5 MB', 'Invalid image file. Only PNG, JPEG, and WebP are allowed', etc.). Profile.jsx passes them directly into showToast() with no i18n wrapping. A Spanish-speaking user who tries to upload an oversized avatar sees English error messages.
      - Fix: The proposed fix works but refactoring the library to return errorKey+errorArgs adds churn across 9 callers including admin and library contexts where translation is irrelevant (checkinPhoto.js throws rather than toasts). A simpler approach: keep validateImageFile returning raw strings as an internal detail, and at each member-facing call site replace `showToast(validation.error, 'error')` with a switch/map from the known error string to a t() call, or add optional i18n overrides at call time. Alternatively, add a thin wrapper in Profile.jsx and other member pages that maps the three possible error strings to their i18n keys before showing the toast, leaving the library unchanged.

- [ ] [race] **handleCheckIn fires reloadActive() without await — rating modal opens before booking status refreshes**  _( My gym, classes, check-in, support, legal, health )_
      - `gym-app/src/pages/Classes.jsx:1093`
      - In `handleCheckIn`, `reloadActive()` is called but not awaited before `setWorkoutPrompt` or `setRatingModal` is called. Both the WorkoutPromptModal and ClassRatingModal are opened immediately, but the booking row in state still has status 'confirmed' (not 'attended'). If the user dismisses the modal and re-opens the detail sheet, the stale booking state can cause the 'Check In' button to re-appear and the attended/rate state to not show until the background reload finally lands.
      - Fix: The proposed `await reloadActive()` at line 1093 is correct, but `setActionLoading(null)` at line 1084 should also be moved to after the await (matching handleBook/handleCancel) so the button stays disabled during the reload. Corrected block: remove `setActionLoading(null)` at line 1084, then replace `reloadActive()` at line 1093 with `await reloadActive(); setActionLoading(null);`.

- [ ] [broken-flow] **gymConfig.supportEmail is never set — Support page always shows platform email regardless of white-label gym**  _( My gym, classes, check-in, support, legal, health )_
      - `gym-app/src/pages/Support.jsx:52`, `gym-app/src/contexts/AuthContext.jsx:378`
      - Support.jsx reads `gymConfig?.supportEmail || DEFAULT_SUPPORT_EMAIL` (line 52) to show the per-gym contact email. However, `setGymConfig()` in AuthContext.jsx (lines 378–396 and 418–420 and 1147) never includes a `supportEmail` key — the gym object does not have this field in the select and it is not mapped. Result: every white-label gym's Support page shows `support@tugympr.com` instead of the gym's own contact, which breaks the white-label promise and misdirects member support requests.
      - Fix: Either (a) add a `support_email` column to gyms and include it in the get_auth_context RPC + gymConfig assembly, or (b) remove the per-gym override if there is no DB backing for it and always use the platform default. If it is intended to be white-labeled, option (a) is required.

- [ ] [i18n] **memberOffers.types missing free_trial, bundle, bring_friend — untranslated raw strings shown to members**  _( My gym, classes, check-in, support, legal, health )_
      - `gym-app/src/pages/MyGym.jsx:522`, `gym-app/src/i18n/locales/en/pages.json`, `gym-app/src/i18n/locales/es/pages.json`
      - MyGym.jsx renders the offer type label as `t('memberOffers.types.${offer.offer_type}', offer.offer_type?.replace(/_/g, ' '))`. The OFFER_TYPE_COLOR map defines three types — `free_trial`, `bundle`, `bring_friend` — that are valid DB values for `gym_offers.offer_type`. Neither `en/pages.json` nor `es/pages.json` has these three keys under `memberOffers.types`. When an admin creates offers of these types, Spanish-speaking members see raw strings like 'free trial', 'bundle', 'bring friend' in the offers badge.
      - Fix: Add `free_trial`, `bundle`, and `bring_friend` keys to `memberOffers.types` in both `en/pages.json` and `es/pages.json`. Suggested ES values: `"free_trial": "Prueba gratis"`, `"bundle": "Paquete"`, `"bring_friend": "Trae un amigo"`.

- [ ] [data-loss] **Lock-screen workout notification permanently shows initial set count**  _( Notifications & push )_
      - `gym-app/src/lib/workoutNotification.js:80`, `gym-app/src/pages/ActiveSession.jsx:11`
      - updateWorkoutNotification() is exported and imported in ActiveSession.jsx but is never called anywhere in the codebase. The module-level lastSetsInfo object is set once by startWorkoutNotification() on workout start and then frozen. The 60-second setInterval in workoutNotification.js reads lastSetsInfo on every tick, so the lock-screen notification body always shows the set count captured at the moment startWorkoutNotification() was called (the initial 0 / N or whatever the live-activity-failed fallback received). A user who completes 15 of 20 sets still sees '0 / 20 sets completed' on their lock screen for the entire workout.
      - Fix: In ActiveSession.jsx, call updateWorkoutNotification(cs, ts) from the same useEffect that calls updateLiveActivity() (around line 1782), inside the catch/fallback branch where startWorkoutNotification was used instead of the Live Activity. Mirror the existing Live Activity update pattern.

- [ ] [ux] **Achievement notifications show generic Bell icon instead of trophy in inbox**  _( Notifications & push )_
      - `gym-app/src/pages/Notifications.jsx:20-35`, `gym-app/src/pages/SessionSummary.jsx:385`
      - SessionSummary.jsx inserts notifications with type: 'achievement' (a valid DB enum value added by migration 0369). The Notifications.jsx TYPE_META map (lines 20-35) does not include an 'achievement' entry, so these notifications fall through to the 'default' case which renders a plain Bell icon in muted text color. Every post-workout badge unlock looks identical to a generic system alert in the notification list.
      - Fix: Add `achievement: { icon: Trophy, color: 'text-[#FF5A2E]', bg: 'bg-[#FF5A2E]/10' }` to the TYPE_META map in Notifications.jsx, reusing the same Trophy/gold styling already applied to pr_beaten.

- [ ] [broken-flow] **Achievement push notifications ignore the user's milestone-alerts preference toggle**  _( Notifications & push )_
      - `gym-app/supabase/functions/send-push-user/index.ts:250-265`, `gym-app/src/pages/SessionSummary.jsx:384-389`, `gym-app/src/pages/NotificationSettings.jsx:22`
      - The send-push-user edge function's NOTIFICATION_TYPE_COLUMNS map (lines 250-265) maps notification types to the per-category preference columns that gate push delivery. 'achievement' is absent from that map. When SessionSummary sends a notification with notification_type='achievement', preferenceColumn resolves to null (line 322), so only the master toggle (notif_push_enabled) is checked — the per-category 'notif_milestone_alerts' toggle is bypassed entirely. A user who disables Milestone Alerts in NotificationSettings will still receive push banners for every badge unlock. This violates Apple Guideline G4.5.4 / G5.1.1 (recipient preferences must suppress sends server-side).
      - Fix: Add `achievement: 'notif_milestone_alerts'` to NOTIFICATION_TYPE_COLUMNS in send-push-user/index.ts, alongside the existing `pr_beaten: 'notif_milestone_alerts'` entry. Both are milestone-class celebrations and should honor the same toggle.

- [ ] [i18n] **aiConsent.medicalDisclaimer missing from both locales — raw key string rendered as legal disclosure**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/pages/progress/ProgressBody.jsx:837`
      - t('aiConsent.medicalDisclaimer') is called with no fallback. The key is absent from both en/pages.json and es/pages.json. The aiConsent object exists in pages.json (title, subtitle, bullets, etc.) but medicalDisclaimer is missing. This text is rendered as a legal/medical disclaimer in the body composition AI section — a blank or raw key here is both an i18n failure and a compliance issue.
      - Fix: Add aiConsent.medicalDisclaimer to en/pages.json (e.g. 'BMI and body-fat estimates are informational only and do not constitute medical advice.') and provide an ES translation. The aiConsent object already exists in both files.

- [ ] [i18n] **share.stickerMode and sessionSummary.share.caption missing from both locales — English inline in ES UI**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/components/share/SimpleShareSheet.jsx:315`, `gym-app/src/components/share/SimpleShareSheet.jsx:322`
      - Two keys added during the share-system rework were never added to either locale file. Both have inline English defaults so Spanish users see English labels in the share panel. share.stickerMode renders 'Sticker mode (overlay on your photo)' and sessionSummary.share.caption renders 'Caption'.
      - Fix: Add "stickerMode": "Sticker mode (overlay on your photo)" under the top-level "share" object in en/pages.json, and "stickerMode": "Sticker sobre tu foto" in es/pages.json. Add "caption": "Caption" under "sessionSummary"."share" in en/pages.json and "caption": "Pie de foto" in es/pages.json. Do not confuse with shareMonth.stickerMode or shareMonth.caption which already exist for a different component.

- [ ] [i18n] **profile.shareMonth missing from both locales — English inline in ES Profile page**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/pages/Profile.jsx:1067`
      - The 'Share this month' button on the Profile page uses a key absent from both locale files. Spanish users see English on this prominent CTA.
      - Fix: Add `"shareMonth": "Share this month"` under the `profile` object in en/pages.json, and `"shareMonth": "Compartir este mes"` under the `profile` object in es/pages.json. The proposed fix is correct.

- [ ] [i18n] **Social feed card kicker strings missing from both locales — English in Spanish social feed**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/components/social/StrataFeedCard.jsx:566`, `gym-app/src/components/social/StrataFeedCard.jsx:576`, `gym-app/src/components/social/StrataFeedCard.jsx:681`, `gym-app/src/components/social/StrataFeedCard.jsx:521`, `gym-app/src/components/social/ShareSheet.jsx:173`
      - Five keys added to StrataFeedCard and social/ShareSheet are absent from both locale files. All have English inline defaults, so Spanish users see English text on every social feed item with a check-in or program-start, on the 'Try this' workout suggestion, on cardio feed entries, and on the share target subtitle. These appear on every social feed render.
      - Fix: Add to pages.json under social.feedContent: checkedInKicker and programKicker (EN/ES). Add social.tryThis, cardio.feedSubtitle, and social.shareSubtitle at their respective top-level locations. The social and cardio objects already exist in both files.

- [ ] [i18n] **sessionSummary.deleteError and sessionSummary.sharePR missing from both locales**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/pages/SessionSummary.jsx:451`, `gym-app/src/pages/SessionSummary.jsx:695`
      - Two keys on the post-workout session summary screen are absent from both locale files. sessionSummary.deleteError is shown in a toast when workout deletion fails; sessionSummary.sharePR is an aria-label on the 'Share PR' button. English defaults are used for both, so Spanish users see English.
      - Fix: Add to sessionSummary in both locale files: deleteError ('Failed to discard workout' / 'No se pudo descartar el entrenamiento') and sharePR ('Share PR' / 'Compartir RP').

- [ ] [contrast] **GoalsSection: hardcoded fixed-light pastel backgrounds (OB_TEAL_SOFT, OB_ORANGE_SOFT) with dark text — jarring in dark mode**  _( Theming, dark mode & contrast (cross-cutting) )_
      - `gym-app/src/components/GoalsSection.jsx:22-27`, `gym-app/src/components/GoalsSection.jsx:342-344`, `gym-app/src/components/GoalsSection.jsx:767-797`, `gym-app/src/components/GoalsSection.jsx:814-832`, `gym-app/src/components/GoalsSection.jsx:865-866`, `gym-app/src/components/GoalsSection.jsx:1272-1279`, `gym-app/src/components/GoalsSection.jsx:1343`, `gym-app/src/components/GoalsSection.jsx:1365-1395`, `gym-app/src/components/GoalsSection.jsx:1443-1444`
      - Several surfaces use hardcoded light-pastel constants that never change with the theme: OB_TEAL_SOFT (#D7F1F1) and OB_ORANGE_SOFT (#FBE0D3). These appear inside a dark-mode ModalShell (bg: var(--color-bg-card) which is near-black in dark) as stark light boxes. Specific occurrences: goal-achieved celebration icon bg (line 342), date-too-aggressive warning banner (767, 814, 1365), selected-exercise chip (1272), 'Suggested date' chip (1343), disabled Save button (865, 1443). The disabled Save button uses `background: OB_TEAL_SOFT, color: OB_SUB (#6B6A63)` — pastel bg with gray text embedded in a dark modal.
      - Fix: Replace fixed pastels with adaptive CSS mix: `OB_TEAL_SOFT` uses -> `color-mix(in srgb, var(--color-accent) 15%, var(--color-bg-card))` with text `var(--color-accent)`. `OB_ORANGE_SOFT` -> `color-mix(in srgb, #FF5A2E 12%, var(--color-bg-card))` with text `#FF5A2E`. For the disabled Save button: `background: 'color-mix(in srgb, var(--color-accent) 25%, var(--color-bg-card))', color: 'var(--color-text-muted)'`.

- [ ] [contrast] **LiveCardio primary action buttons use `color: var(--color-bg-card, #0A0D10)` as text on accent bg — wrong in light mode**  _( Theming, dark mode & contrast (cross-cutting) )_
      - `gym-app/src/pages/LiveCardio.jsx:907`, `gym-app/src/pages/LiveCardio.jsx:1042`, `gym-app/src/pages/LiveCardio.jsx:1117`, `gym-app/src/pages/LiveCardio.jsx:1188`, `gym-app/src/pages/LiveCardio.jsx:1271`
      - Start, Pause/Resume, Finish & Log, unit badge, and Share buttons all use `color: 'var(--color-bg-card, #0A0D10)'` as text color on an accent-colored background. In dark mode --color-bg-card is near-black (correct, dark text on amber/teal works). In light mode --color-bg-card resolves to near-white (approx hslToHex(h, 4, 100)). A gym with stealth_mono palette (primary #E4E4E7, a very light gray) would produce near-white text on near-white accent background — invisible text. The branding pipeline already computes `--color-text-on-accent` via WCAG luminance check (textOnColor()) and sets it on every applyBranding call.
      - Fix: Replace all `color: 'var(--color-bg-card, #0A0D10)'` on accent-background buttons with `color: 'var(--color-text-on-accent, #1D1D1F)'`. This variable is set by applyBranding/applyGymTheme and is the canonical WCAG-aware text color for accent surfaces.

- [ ] [broken-flow] **Messages conversation load — no try/catch on load(), loading stuck on error**  _( Data layer, RLS assumptions & error handling (cross-cutting) )_
      - `gym-app/src/pages/Messages.jsx:542-595`
      - The load() async function inside the conversation useEffect has no try/finally. Any error in the sequence (conv fetch, profile fetch, messages fetch, decryptMessage) will leave setLoading(true) — the chat thread stays on a permanent loading skeleton with no way to recover. The cancelled guard only prevents stale state updates; it does not prevent loading from being stuck on error.
      - Fix: The proposed try/finally fix is correct. Additionally, the early return at line 556 should explicitly call setLoading(false) before returning: `if (cancelled) return; if (!conv) { setLoading(false); return; }` — separating the two cases so cancelled unmount skips the state update (correct) while a null conv properly clears the spinner. The try/finally alone also solves it since finally always runs regardless of early return.

## P3 (26)

- [ ] [broken-flow] **AgeVerificationScreen component defined but never rendered — age gate silently absent**  _( Auth, onboarding & deep links )_
      - `gym-app/src/App.jsx:483`, `gym-app/src/App.jsx:808`, `gym-app/src/App.jsx:830`, `gym-app/src/contexts/AuthContext.jsx:1313`
      - requiresAgeVerification is computed in AuthContext (true when profile has no date_of_birth AND no age_verified_at) and destructured in all four route guards (ProtectedRoute, OnboardingRoute, AdminRoute, TrainerRoute), but every usage site has the rendering call commented out with 'Runtime age gate removed'. The AgeVerificationScreen component still exists at line 483 and is never rendered anywhere. Legacy accounts that predate the DOB-at-signup requirement (before migration 0344) and that have neither date_of_birth nor age_verified_at set can reach the full member experience without any age check.
      - Fix: If the decision to remove the runtime gate is final and backfilled DOBs exist for all legacy users, delete the AgeVerificationScreen component, the requiresAgeVerification computation, and remove it from all useAuth() destructures to avoid dead code confusion. If the gate is intentionally deferred but legacy users remain, add a one-time prompt inside ProtectedRoute using the requiresAgeVerification flag before the is_onboarded check.

- [ ] [data-loss] **enrollInTemplate uses non-atomic routine cleanup — all old 'Auto:' routines deleted before new ones created, leaving a window with no active program routines**  _( Workout generation & routines )_
      - `gym-app/src/pages/Workouts.jsx:1411-1426`
      - When the user switches to a gym program template, enrollInTemplate deletes all existing 'Auto:' routines (and their exercises + schedule entries) at lines 1411-1426 before creating the new routines. If the network fails mid-way through routine creation at lines 1604-1657, the user ends up with no routines in their program — the old ones are deleted and the new insert failed. The generated_programs row is already inserted at line 1558 pointing to routines that don't exist yet. A reload shows the program card with no routines. This is a data-loss window that can strand the user.
      - Fix: The proposed fix direction is correct: create new routines first (lines 1604-1657 logic moved earlier), then insert the `generated_programs` row (line 1558), then delete old `Auto:` routines (lines 1418-1427). This ensures that at no point does the user have a program row with no routines. The old program should be expired only after the new program and routines are fully committed.

- [ ] [data-loss] **Calorie calculation inconsistency: manual CardioLogModal applies intensity multiplier, live GPS tracking does not**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/components/CardioLogModal.jsx:101-102`, `gym-app/src/pages/LiveCardio.jsx:540`
      - In CardioLogModal (manual log), estimated calories apply an intensity multiplier (easy 0.75×, moderate 1.0×, hard 1.25×, max 1.5×) when no distance is provided (line 102). In LiveCardio, calories are computed with `estimateCardioCalories(..., { requireMovement: useGps })` and no intensity multiplier is ever applied. The same user doing the same 30-minute HIIT session would get different calorie values depending on whether they used the live tracker or the manual log modal. This is not a crash but produces inconsistent data.
      - Fix: The proposed fix is correct. In LiveCardio, after computing `cal` at line 540, apply the multiplier only when there is no distance (matching the CardioLogModal condition): `const intensityMult = { easy: 0.75, moderate: 1.0, hard: 1.25, max: 1.5 }[intensity] ?? 1.0; const cal = !distanceKm ? Math.round(rawCal * intensityMult) : rawCal;` where `rawCal` is the result of `estimateCardioCalories(...)`. For GPS sessions with distance, `estimateCardioCalories` already applies a speed-derived intensity multiplier internally (line 59 of cardioCalories.js: `0.7 + 0.6 * Math.min(speedRatio, 2)`), so the manual multiplier should only apply when `!distanceKm`. The category should be corrected to "ux/accuracy" rather than "data-loss".

- [ ] [data-loss] **GPS tracker elapsedSec misses permission-request latency for granted sessions**  _( Cardio (live run/route, logging, detail) )_
      - `gym-app/src/pages/LiveCardio.jsx:423-479`, `gym-app/src/lib/gpsTracker.js:297`
      - When the user taps Start for a GPS activity, `handleStart()` immediately calls `startTimerOnlyFallback()` (line 431) which sets `running=true`. However, the timer interval (lines 222-241) skips because `useGps=true`. The display shows 00:00:00 during the 5-15 second GPS permission dialog. When permission is granted and `tracker.start()` is finally called, the tracker sets `state.startedAt = Date.now()` (line 297 in gpsTracker.js), missing the elapsed time waiting for permission. The GPS session's elapsed time is shorter than actual wall time by the permission-request delay.
      - Fix: The proposed fix is correct in spirit. The simplest implementation uses the already-set startRef.current (set by startTimerOnlyFallback at line 418 before the async GPS block begins): when tracker.start() succeeds at line 474, pass elapsedOffsetSec: (Date.now() - startRef.current) / 1000 in the createGpsTracker call at line 448. Since startRef.current is set before requestPermissions() is awaited, this accurately captures the permission-wait latency without needing a new variable.

- [ ] [ux] **MonthlyProgressReport weight gain/loss indicator is goal-blind — always marks weight gain as red**  _( Progress, metrics, PRs, charts )_
      - `gym-app/src/components/MonthlyProgressReport.jsx:739-741`, `gym-app/src/components/MonthlyProgressReport.jsx:771-774`
      - The Monthly Report always colors weight gain red and weight loss green, ignoring the user's goal. A user on a muscle-gain program who intentionally gained 3 lbs sees their progress flagged in red. The Body tab already has `getProgressColor(metric, delta, primaryGoal)` that correctly handles this — the monthly report skips it. The same issue affects circumference measurements (e.g., waist going up is always shown as red even during a bulk where it's expected).
      - Fix: The proposed fix is directionally correct but the file reference is wrong: `getProgressColor` is a non-exported local const in ProgressBody.jsx, not in any progressConstants.js. The fix must either (1) extract `getProgressColor` into a new shared util (e.g., `src/lib/progressUtils.js`) and import it in both files, or (2) inline the same logic inside MonthlyProgressReport. In either case, also add `primary_goal` to the member_onboarding select at line 282 and thread it through to the data object so it is available at render time.

- [ ] [ux] **ExerciseProgressChart dashed bridge line between last real 1RM and projected point does not render**  _( Progress, metrics, PRs, charts )_
      - `gym-app/src/components/ExerciseProgressChart.jsx:111-122`, `gym-app/src/components/ExerciseProgressChart.jsx:205-216`
      - The chart appends a `bridge` data field only to the terminal projected element. A Recharts Line with connectNulls:false and only one non-null data point cannot draw a line segment — it renders as an invisible isolated point. The intended dashed line connecting the last real 1RM point to the projected dot is therefore never visible. The projected dot still appears (via ProjectedDot), so data accuracy is preserved, but the dashed visual bridge that communicates 'this is where we project you'll be' is silently missing.
      - Fix: The proposed fix is correct. Apply `bridge` to both the last real element and the projected element: replace the chartData construction (lines 111-122) with: `const chartData = data.length > 0 ? [ ...data.slice(0, -1), { ...data[data.length-1], bridge: data[data.length-1].orm }, { date: t('strength.fourWeekProjLabel', '~4 wks'), orm: null, projected: projectedOrm, bridge: projectedOrm } ] : data;` This gives two consecutive non-null bridge values so Recharts draws the dashed segment.

- [ ] [i18n] **Missing i18n keys goals.units.workouts and goals.units.days — English shown in Spanish UI**  _( Goals & achievements )_
      - `gym-app/src/components/GoalsSection.jsx:50-54`, `gym-app/src/i18n/locales/es/pages.json`, `gym-app/src/i18n/locales/en/pages.json`
      - The `unitLabel()` helper (line 50-54) calls `t('goals.units.${u}', u)` to display localized unit labels. The `goals.units` section is entirely absent from both locale files. For `lbs` and `%` the English fallback is acceptable (locale-neutral), but `workout_count` goals show the English word 'workouts' and `streak` goals show the English word 'days' in the Spanish UI — both should be 'entrenamientos' and 'días' respectively. The unit label is displayed next to the number input in the GoalModal details form and next to the delta chip in GoalCard.
      - Fix: Add `goals.units` to both locale files:
- EN: `{ "goals": { "units": { "lbs": "lbs", "%": "%", "workouts": "workouts", "days": "days" } } }`
- ES: `{ "goals": { "units": { "lbs": "lbs", "%": "%", "workouts": "entrenamientos", "days": "días" } } }`

- [ ] [broken-flow] **DM decryption race: messages arriving during conversation load render as silent empty bubbles**  _( Social feed, community, messages, moderation )_
      - `gym-app/src/pages/Messages.jsx:481`, `gym-app/src/pages/Messages.jsx:603-643`
      - The realtime Supabase channel for incoming DMs is subscribed immediately when the ChatView mounts (one useEffect keyed on conversationId), while the encryption seed is fetched asynchronously in a separate load() function. If a message arrives from the other party during the network round-trip for the conversation row, encryptionSeedRef.current is still null. decryptMessage is called with the wrong key (derived from 'null' + conversationId), AES-GCM decryption throws, the catch block at line 634 inserts the message with body: '' (empty string) into local state, and there is no retry mechanism. The empty bubble remains permanently until the user closes and reopens the conversation. It is especially likely to trigger during active back-and-forth messaging when one side opens the thread.
      - Fix: The proposed fix is directionally correct. The simplest correct approach is to move the realtime subscription setup to run after the seed is loaded: at the end of load() after setting encryptionSeedRef.current, start the channel and store its cleanup in a channelRef, then clean up in the existing realtime useEffect's return or a separate cleanup effect. Alternatively, add a pendingRawRef array: in the realtime handler, if encryptionSeedRef.current is null, push payload.new to pendingRawRef; at the end of load() after setting the seed, drain pendingRawRef by decrypting each raw payload and prepending to messages (before setMessages(decrypted) so the initial batch deduplicates them via the id-already-present check). Note: the empty-bubble description in the finding is inaccurate — the actual visible artifact is '[Unable to decrypt]' text because decryptMessage never rejects.

- [ ] [i18n] **ReportContentModal title noun not localized in Spanish (moderation.contentNoun missing from both locales)**  _( Social feed, community, messages, moderation )_
      - `gym-app/src/components/ReportContentModal.jsx:62-64`, `gym-app/src/i18n/locales/en/pages.json`, `gym-app/src/i18n/locales/es/pages.json`
      - ReportContentModal resolves a noun (post/comment/message/profile) for the modal title via `t('moderation.contentNoun.${NOUN_KEY[contentType]}', { defaultValue: NOUN_KEY[contentType] || 'post' })`. The moderation.contentNoun namespace does not exist in either locale, so the noun falls back to the raw English strings 'post', 'comment', 'message', 'profile'. These are then interpolated into the ES title template 'Reportar este/a {{noun}}', producing 'Reportar este/a post', 'Reportar este/a comment', etc. instead of the correct Spanish words publicación, comentario, mensaje, perfil.
      - Fix: Add moderation.contentNoun to both locales. EN: { post: 'post', comment: 'comment', message: 'message', profile: 'profile' }. ES: { post: 'publicación', comment: 'comentario', message: 'mensaje', profile: 'perfil' }.

- [ ] [data-loss] **Mute-user state stored only in localStorage — silently lost when iOS WKWebView clears storage**  _( Social feed, community, messages, moderation )_
      - `gym-app/src/pages/SocialFeed.jsx:39-46`, `gym-app/src/pages/SocialFeed.jsx:935`, `gym-app/src/pages/SocialFeed.jsx:1243-1246`
      - The mute feature stores the muted user ID list exclusively in localStorage under the key 'social_muted_users'. iOS WKWebView is known to evict localStorage under low-storage conditions and between app restarts after memory warnings. When this happens, all mutes are silently lost with no error or recovery path. The user sees muted users reappear in their feed with no notification. Unlike block (which persists to the blocked_users table with RLS), mute has no server-side record. On the member's second device mutes also do not sync.
      - Fix: Add a muted_users table (or a jsonb column on profiles) and upsert when muting. On SocialFeed mount, load muted IDs from DB alongside blocked IDs. The existing handleMute callback at line 1243 can fire-and-forget a DB upsert just like handleHide does for hidden_posts.

- [ ] [i18n] **Hardcoded 'YOU' badge is not translated in expanded leaderboard list**  _( Leaderboard )_
      - `gym-app/src/pages/Leaderboard.jsx:322`
      - Inside ExpandedListRow (line 322), the self-identifier badge renders the literal string 'YOU' instead of using the translation key leaderboard.you (already defined as 'Tú' in the ES locale). Spanish-UI members see an untranslated English badge on their own row in every expanded leaderboard board.
      - Fix: Change `>YOU</span>` to `>{t('leaderboard.you')}</span>`. The t function is already available in the outer ExpandedList and passed into rowProps (line 404), so ExpandedListRow already receives it.

- [ ] [i18n] **Multiple missing Spanish diacritics in challengeFriend ES locale keys**  _( Leaderboard )_
      - `gym-app/src/i18n/locales/es/pages.json:leaderboard.challengeFriend`
      - Several Spanish strings in the challenge modal i18n keys are missing accent marks (tildes), producing grammatically incorrect Spanish text visible to Spanish-UI users when they open the challenge modal from the leaderboard.
      - Fix: Fix all diacritics in the es/pages.json leaderboard.challengeFriend block: 'dias' → 'días', 'Metrica' → 'Métrica', 'Duracion' → 'Duración', 'reto!' → 'retó!', 'esta activo' → 'está activo', 'rechazo' → 'rechazó', 'gano' → 'ganó', 'proxima' → 'próxima', 'esta semana' → 'esta semana' (ok), 'Records Personales' → 'Récords Personales'.

- [ ] [other] **Dead sub-components (MilestoneFeed, ConsistencyList, MostImprovedList, BoardSelector, TierFilter) never imported**  _( Leaderboard )_
      - `gym-app/src/components/leaderboard/MilestoneFeed.jsx`, `gym-app/src/components/leaderboard/ConsistencyList.jsx`, `gym-app/src/components/leaderboard/MostImprovedList.jsx`, `gym-app/src/components/leaderboard/BoardSelector.jsx`, `gym-app/src/components/leaderboard/TierFilter.jsx`
      - All five component files under gym-app/src/components/leaderboard/ are never imported anywhere in the codebase. Leaderboard.jsx is fully self-contained and renders everything inline. MostImprovedList.jsx at line 89 also has an outdated score display (`+{entry.score}%`) that would show a percentage for what is now an absolute lbs delta — but since it's dead code this doesn't affect users.
      - Fix: Delete the five unused files to avoid confusion and prevent the stale score display in MostImprovedList from being accidentally re-activated. Alternatively, keep them as reference but add a comment noting they are unused.

- [ ] [ux] **DailySuggestion does not regenerate when macro targets change mid-day**  _( Nutrition & meal plans )_
      - `gym-app/src/pages/Nutrition.jsx:1826-1832`
      - The `DailySuggestion` component (daily AI meal suggestion panel) has a `useEffect` that generates a day plan using `generateDayPlan`. The effect has an early-exit guard `if (meals) return;` that prevents regeneration once any suggestions exist. If a member opens the Nutrition tab, gets suggestions, then edits their macro targets via `TargetEditModal` and saves new targets, the suggestions continue to reflect the old targets for the rest of the day. The user has no manual refresh control and the plan auto-refreshes only on midnight UTC (new `storageKey`). This is low severity because the mismatch is temporary and doesn't cause data loss.
      - Fix: The simplest fix is to wire the already-written `handleRegenerate` to a refresh button in the DailySuggestion header (e.g., a small refresh icon next to the "AI" badge). This avoids any storage-key or hash complexity. The targets-hash approach in the proposal also works but is more complex than needed given `handleRegenerate` is already correct and ready to use.

- [ ] [i18n] **profile.shareMonth i18n key is missing from both locales — Spanish UI shows English button label**  _( Profile, settings, account, photos )_
      - `gym-app/src/pages/Profile.jsx:1067`, `gym-app/src/i18n/locales/en/pages.json`, `gym-app/src/i18n/locales/es/pages.json`
      - Profile.jsx renders `t('profile.shareMonth', { defaultValue: 'Share this month' })` for the monthly recap share button. The key `profile.shareMonth` does not exist in either locale file. The top-level `shareMonth` namespace covers the share sheet itself, but `profile.shareMonth` (the button label) is missing. In both locales the button silently falls back to 'Share this month' (English).
      - Fix: Add `"shareMonth": "Share this month"` under the `profile` object in `en/pages.json` and `"shareMonth": "Compartir este mes"` in `es/pages.json`.

- [ ] [ux] **AvatarPicker does not reset selection state on re-open — user sees their unsaved (previously abandoned) avatar choice**  _( Profile, settings, account, photos )_
      - `gym-app/src/components/AvatarPicker.jsx:49-51`, `gym-app/src/components/AvatarPicker.jsx:57-62`
      - selectedType and selectedValue are initialized from currentAvatar props via useState (runs only once on mount). The isOpen useEffect (lines 57-62) only manages body scroll, not state. If a user opens the picker, selects a new color/design, closes without saving (X or backdrop), then reopens, they see their unsaved selection — not their actual current avatar. The preview avatar and active checkmark both reflect the abandoned state. If they then tap Save without changing anything they overwrite their real avatar with the wrong choice.
      - Fix: Add a reset effect: `useEffect(() => { if (isOpen) { setTab(currentAvatar?.type || 'color'); setSelectedType(currentAvatar?.type || 'color'); setSelectedValue(currentAvatar?.value || '#6366F1'); setPreviewFile(null); setPreviewUrl(null); } }, [isOpen]);` (also revoke any prior previewUrl in the cleanup).

- [ ] [ux] **AI consent revoke toast uses label text 'Not granted' instead of a revocation confirmation**  _( Profile, settings, account, photos )_
      - `gym-app/src/pages/MemberSettings.jsx:94`
      - After the user revokes AI photo consent, handleRevokeAIConsent shows `showToast(t('settingsPrivacy.consentNotGranted'), 'success')`. The key `settingsPrivacy.consentNotGranted` translates to 'Not granted' (EN) / 'No concedido' (ES) — this is the row status label, not a user-facing action confirmation. The success toast reads 'Not granted' which is semantically confusing; the user tapped a toggle and the system responded with a state label rather than an action acknowledgement.
      - Fix: Add a dedicated key `settingsPrivacy.consentRevoked` (EN: 'AI consent removed', ES: 'Consentimiento retirado') and use it in handleRevokeAIConsent instead of consentNotGranted.

- [ ] [data-loss] **Monthly share card uses the 50-row capped PRs array ordered by 1RM — may undercount this month's PRs for users with many PRs**  _( Profile, settings, account, photos )_
      - `gym-app/src/pages/Profile.jsx:456-462`, `gym-app/src/pages/Profile.jsx:1781-1783`
      - Personal records are fetched ordered by `estimated_1rm DESC LIMIT 50`. The monthly share card filters this array by achieved_at date to produce the monthly PR count. Any PR achieved this month that did not land in the top-50 by estimated_1rm is silently excluded, causing the share card to report fewer PRs than the user actually earned.
      - Fix: Either (a) order by `achieved_at DESC` so the most recent PRs are always included, or (b) add a second query specifically for monthly PRs that uses `achieved_at >= month_start` with no 1RM sort, and use that for the share card only.

- [ ] [broken-flow] **LegalViewer iframe onError does not fire for cross-origin blocks in iOS WKWebView — blank white page with no feedback**  _( My gym, classes, check-in, support, legal, health )_
      - `gym-app/src/pages/LegalViewer.jsx:179-188`, `gym-app/src/pages/LegalViewer.jsx:77-80`
      - LegalViewer embeds `https://tugympr.com/privacy` and `/terms` in an `<iframe>`. In iOS WKWebView (Capacitor), cross-origin iframes that are blocked by WKWebView's content policy or by the remote server's CSP/X-Frame-Options headers do NOT fire the iframe's `onerror` event — they fire `onload` with a blank document or never fire at all. The 10-second safety timeout on line 79 only sets `loaded=true` (hides the spinner), but never sets `error=true`, so the member sees a permanently white `#FFFFFF` content area with no 'Open in browser' escape hatch. Additionally the memory notes the share domain `tugympr.app` is dead — if `tugympr.com` is similarly not live, every member hits this blank state.
      - Fix: Change the 10s safety timeout to also set `error=true` when the iframe content is blank (or replace with `setError(true)` so the 'Open in browser' button appears). Consider using `@capacitor/browser` to open legal pages natively instead of an iframe on native builds. Also fix the inner `div` background from `#FFFFFF` to `var(--color-bg-primary)` to avoid dark-mode white flash.

- [ ] [other] **Production debug console.log statements left in healthSync.js sleep tracking**  _( My gym, classes, check-in, support, legal, health )_
      - `gym-app/src/lib/healthSync.js:629`, `gym-app/src/lib/healthSync.js:634`, `gym-app/src/lib/healthSync.js:656`, `gym-app/src/lib/healthSync.js:691`
      - Four `console.log` calls with debug comments ('DEBUG: surface what the plugin returned. Remove once verified.') are present in `readSleepLastNight()`. These log full HealthKit sample arrays to the console on every sleep read, exposing personal health data in crash logs and developer consoles. The inline comment at line 629 explicitly marks them for removal.
      - Fix: Remove all four `console.log` calls from `readSleepLastNight()`. Replace with `logger.debug?.(...)` calls if debug logging is still needed, but these should not ship to App Store.

- [ ] [race] **deleteNotification revert snapshot can silently discard a concurrently inserted notification**  _( Notifications & push )_
      - `gym-app/src/pages/Notifications.jsx:132-152`
      - deleteNotification() captures `const snapshot = items` at the top of the callback. If the Supabase update fails and error handling executes `setItems(snapshot)` to revert, any notification that arrived via the realtime INSERT subscription between the optimistic removal and the revert is silently dropped — the snapshot pre-dates the new arrival, so restoring it overwrites the newly prepended row. The user sees their new notification flash in and then disappear.
      - Fix: On error revert, re-add the deleted item rather than fully restoring the snapshot: `setItems(prev => prev.some(n => n.id === id) ? prev : [snapshot.find(n => n.id === id), ...prev].filter(Boolean));` This preserves any concurrently inserted rows while still reinstating the item that failed to delete.

- [ ] [i18n] **activeSession.openExerciseList missing from both locales — English in active workout header**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/pages/active-session/SessionHeader.jsx:273`
      - The aria-label for the 'See all exercises' button in the live workout header is absent from both locale files. An inline English default is used, so Spanish users see an English accessibility label. While visually harmless (aria-label), it breaks screen-reader accessibility for Spanish speakers.
      - Fix: Add activeSession.openExerciseList to en/pages.json ('See all exercises') and es/pages.json ('Ver todos los ejercicios'). The activeSession object already exists in both files.

- [ ] [i18n] **exerciseLibrary.filter missing from both locales — English button in exercise filter sheet**  _( i18n completeness (cross-cutting) )_
      - `gym-app/src/components/MuscleExercisesSheet.jsx:257`
      - The 'Filter' button label in the exercise library sheet has an inline English default and is absent from both locale files. Spanish users see an English button.
      - Fix: Add exerciseLibrary.filter to en/pages.json ('Filter') and es/pages.json ('Filtrar'). The exerciseLibrary object already exists in both files.

- [ ] [contrast] **GoalsSection: 3 raw OB_LINE borders without var(--color-border) fallback — invisible in dark mode**  _( Theming, dark mode & contrast (cross-cutting) )_
      - `gym-app/src/components/GoalsSection.jsx:337`, `gym-app/src/components/GoalsSection.jsx:1283`, `gym-app/src/components/GoalsSection.jsx:1294`
      - OB_LINE is `rgba(11,15,18,0.08)` — a nearly-transparent dark overlay that creates a subtle border on light backgrounds. In dark mode the base canvas is dark, so an 8% dark-on-dark overlay is perceptually invisible (0 border). Three usages apply it raw without the `var(--color-border, OB_LINE)` pattern used correctly in the other 5 instances in the same file.
      - Fix: The proposed fix (`var(--color-border, OB_LINE)`) is functionally identical to the current code because `--color-border` is never defined in index.css. The correct fix is to use the variables that ARE defined for dark mode: `var(--color-border-default, rgba(255,255,255,0.07))` for these borders, or to reference `--color-border-default` (which maps to `rgba(0,0,0,0.09)` in light and `rgba(255,255,255,0.07)` in dark). Apply this fix to all 8 OB_LINE usages in the file (lines 289, 337, 408, 519, 561, 1197, 1283, 1294), not just the 3 cited ones.

- [ ] [contrast] **Leaderboard podium: hardcoded AVATAR_COLORS ['#6B7280','#F59E0B','#2EC4C4'] ignore gym palette; 3rd slot clashes on non-teal gyms**  _( Theming, dark mode & contrast (cross-cutting) )_
      - `gym-app/src/pages/Leaderboard.jsx:1016`, `gym-app/src/pages/Leaderboard.jsx:1110-1121`, `gym-app/src/pages/Leaderboard.jsx:311`
      - Podium carousel uses `AVATAR_COLORS = ['#6B7280', '#F59E0B', '#2EC4C4']` for the avatar ring backgrounds of ranks 2, 1, 3 respectively. Slot 3 (#2EC4C4) is hardcoded TuGymPR default teal. On a crimson_power gym (#EF4444 primary), the 3rd-place avatar background is a mismatched teal, not brand-coherent. ExpandedListRow at line 311 repeats the same pattern: `background: rank <= 3 ? ['#F59E0B','#6B7280','#2EC4C4'][rank-1] : 'var(--color-bg-elevated)'`. Both locations also hardcode `color: '#fff'` for the initial letter on these backgrounds; #F59E0B (amber) + white letter is fine, but #6B7280 (gray) + white is borderline (3.5:1 ratio, passes AA for large text but marginal).
      - Fix: Change slot indices to use brand-aware tokens: rank-1 (gold) -> `'#FFD166'` (semantic gold for 1st, not gym color, intentional), rank-2 (silver) -> `'var(--color-border-strong, #888)'`, rank-3 (bronze) -> `'color-mix(in srgb, var(--color-accent) 60%, #8B4513)'` or simply `'#CD7F32'` (standard bronze). In ExpandedListRow, the same substitution applies.

- [ ] [broken-flow] **usePaginatedQuery silently swallows query errors — no error state exposed to callers**  _( Data layer, RLS assumptions & error handling (cross-cutting) )_
      - `gym-app/src/hooks/usePaginatedQuery.js:64-68`
      - When a Supabase query fails, loadPage() just calls setLoading(false) and returns — no error state is set, no error is thrown. The hook returns no `error` field in its public API. Any page using this hook will show an empty list with no indication of failure. Users see a blank UI and have no way to retry. This is especially harmful for member-facing features using this hook.
      - Fix: Add an `error` state: `const [error, setError] = useState(null)`. Set it on failure and clear it on success. Return it from the hook so callers can render an error/retry UI. This hook is not currently used by any page (grep confirms zero callers outside its own file), but it should be fixed before adoption.

