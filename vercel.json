{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "buildCommand": "cd gym-app && npm install && npm run build",
  "outputDirectory": "gym-app/dist",
  "installCommand": "echo skip"
}
