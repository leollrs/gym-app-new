.vercel

# macOS metadata
._*
.DS_Store

# Source video files (compressed versions live in public/videos)
gym-app/videos/
*.mov
*.MP4
Lateral_raises_video.mp4

# Environment files
.env
.env.*
.env.local
.env.production
!.env.example

# Firebase / Google services config
google-services.json
GoogleService-Info.plist

# Private keys
*.key
*.pem
*.keystore
*.jks

# Claude Code
.claude/worktrees/
.claude/scheduled_tasks.lock
.audit-findings.md

# Local test/audit screenshots (root-level only — keep app assets tracked)
/*.png
/audit-*.png

# MCP server configs + temp captures (local-only)
.mcp.json
.playwright-mcp/

# Supabase CLI local state (keep config.toml tracked, ignore runtime temp)
supabase/.temp/
supabase/.branches/
